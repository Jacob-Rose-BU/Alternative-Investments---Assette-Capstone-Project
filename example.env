# Snowflake connection configuration
SNOWFLAKE_USER=""           # Your Snowflake username (e.g., email or login name)
SNOWFLAKE_PASSWORD=""       # Your Snowflake password (keep secure!)
SNOWFLAKE_ACCOUNT=""        # Your full Snowflake account identifier (e.g., xy12345.us-east-1)
SNOWFLAKE_ROLE=""           # Default role to use for your session (e.g., SYSADMIN, ACCOUNTADMIN, ANALYST)
SNOWFLAKE_WAREHOUSE=""      # Virtual warehouse used to execute queries (e.g., COMPUTE_WH)
SNOWFLAKE_DATABASE=""       # Target database where your tables will be created (e.g., FUNDDATA_DB)
SNOWFLAKE_SCHEMA=""         # Schema inside the database to use (e.g., PUBLIC, REPORTING)

# GPT API Key for content generation
OPENROUTER_API_KEY=""       # API key for accessing GPT models via OpenRouter (used for factsheet narratives, etc.)
